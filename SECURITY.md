# Security notes

Nunca commitar service role, DATABASE_URL, senha ou token administrativo. `config.js` pode conter apenas URLs e configurações públicas. Tokens de briefing são credenciais temporárias e devem ser enviados individualmente. O link não deve ser encaminhado para terceiros.
